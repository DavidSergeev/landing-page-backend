# Core AI / LangChain stack
langchain-core==1.3.0
langchain-google-genai==4.2.2
langgraph==1.1.9
langgraph-checkpoint==4.0.2
langgraph-prebuilt==1.0.10
langgraph-sdk==0.3.13
langsmith==0.7.33

# AWS Lambda streaming response support
# The managed Python runtime has no native RESPONSE_STREAM support, so the app
# runs as a plain ASGI server behind the AWS Lambda Web Adapter (see run.sh /
# template.yaml) instead of a bare handler function.
fastapi>=0.115.0
uvicorn>=0.32.0

# HTTP client (used by research module and LangChain internals)
requests>=2.31.0

# Environment variable loading (.env file)
python-dotenv==1.2.2

# AWS SDK — DynamoDB client
boto3>=1.34.0